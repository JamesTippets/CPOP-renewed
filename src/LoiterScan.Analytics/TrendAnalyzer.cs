namespace LoiterScan.Analytics;

/// <summary>Cross-run trends (events per run, new vs recurring, breakdowns by regime/owner),
/// scoped to a fixed configuration so threshold changes don't confound the trend.</summary>
public sealed class TrendAnalyzer
{
    // TODO: per-run aggregation, config-scoped (spec section 8.2).
}
