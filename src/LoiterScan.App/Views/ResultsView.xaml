<UserControl x:Class="LoiterScan.App.Views.ResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Top" Margin="0 0 0 8">
            <TextBlock Text="{Binding RunLabel}" FontSize="16" FontWeight="Bold"/>
            <TextBlock Text="{Binding EventCount, StringFormat='{}{0} loitering event(s) detected'}"
                       Foreground="#555" Margin="0 2 0 0"/>
        </StackPanel>

        <DataGrid x:Name="ResultsGrid"
                  ItemsSource="{Binding Events}"
                  SelectedItem="{Binding SelectedEvent}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  VirtualizingStackPanel.IsVirtualizing="True"
                  EnableRowVirtualization="True"
                  MouseLeftButtonUp="ResultsGrid_MouseLeftButtonUp"
                  PreviewMouseRightButtonDown="ResultsGrid_PreviewMouseRightButtonDown">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Exclude pair from future runs"
                              Command="{Binding PlacementTarget.DataContext.ExcludePairCommand,
                                        RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                              CommandParameter="{Binding PlacementTarget.SelectedItem,
                                                RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Pair Key"         Binding="{Binding PairKeyDisplay}" Width="110"/>
                <DataGridTextColumn Header="Sat A"            Binding="{Binding SatADisplay}" Width="180"/>
                <DataGridTextColumn Header="Sat B"            Binding="{Binding SatBDisplay}" Width="180"/>
                <DataGridTextColumn Header="Duration (min)"    Binding="{Binding DurationMinutes, StringFormat='F0'}" Width="110"/>
                <DataGridTextColumn Header="Min Distance (km)" Binding="{Binding MinRangeKm,  StringFormat='F2'}" Width="115"/>
                <DataGridTextColumn Header="R (km)"            Binding="{Binding CaRicR,       StringFormat='F2'}" Width="75"/>
                <DataGridTextColumn Header="I (km)"            Binding="{Binding CaRicI,       StringFormat='F2'}" Width="75"/>
                <DataGridTextColumn Header="C (km)"            Binding="{Binding CaRicC,       StringFormat='F2'}" Width="75"/>
                <DataGridTextColumn Header="Close Approach (UTC)" Binding="{Binding CloseApproachUtc, StringFormat='yyyy-MM-dd HH:mm'}" Width="160"/>
                <DataGridTextColumn Header="Loiter Start (UTC)"   Binding="{Binding LoiterStartUtc,  StringFormat='yyyy-MM-dd HH:mm'}" Width="155"/>
                <DataGridTextColumn Header="Confidence"       Binding="{Binding Confidence,  StringFormat='P0'}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
